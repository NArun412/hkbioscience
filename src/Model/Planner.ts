export interface Planner {
  name: string;
  industry: string;
  cleanroomClass: string;
  email?: string;  // Make email optional
  text?: string;   // Add text as optional
  projectStartTime: string;

}
